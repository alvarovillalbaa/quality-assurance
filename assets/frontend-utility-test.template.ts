// import { utilityFunction } from './utilityFunction'

describe('utilityFunction', () => {
  it('returns the expected value for the main case', () => {
    // expect(utilityFunction(...)).toEqual(...)
  })

  test.each([
    // [input, expected]
  ])('maps %p to %p', (input, expected) => {
    // expect(utilityFunction(input)).toEqual(expected)
  })

  it('handles edge input without mutation', () => {
    // const input = ...
    // const snapshot = structuredClone(input)
    // utilityFunction(input)
    // expect(input).toEqual(snapshot)
  })
})
